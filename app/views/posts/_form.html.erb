<style type="text/css">
  #post_body{
    width: 100%;
    height: 300px;
    border: 1px solid orange;
    background-color: transparent;
    color: white;
  }
    #post_title{
    width: 100%;
    height: 70px;
    border: 1px solid orange;
    background-color: transparent;
    color: white;
  }
  .orange{
    font-weight: 900;
    color: orange;
  }
</style>
<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title, class: 'orange' %><br>
    <%= form.text_field :title, id: :post_title %>
  </div><br><br>

  <div class="field">
    <%= form.label :body, class: 'orange' %><br>
    <%= form.text_area :body, id: :post_body %>
  </div><br><br>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
