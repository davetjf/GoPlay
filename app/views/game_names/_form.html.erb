<%= form_with(model: game_name, local: true) do |form| %>
  <% if game_name.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game_name.errors.count, "error") %> prohibited this game_name from being saved:</h2>

      <ul>
      <% game_name.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :game_name_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
